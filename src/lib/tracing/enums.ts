export enum StepType {
  USER_CALL = 'user_call',
  CHAT_COMPLETION = 'chat_completion',
  CHAIN = 'chain',
  AGENT = 'agent',
  TOOL = 'tool',
  RETRIEVER = 'retriever',
  FUNCTION_CALL = 'function_call',
}
